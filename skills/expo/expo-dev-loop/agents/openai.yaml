interface:
  display_name: "Expo Dev Loop"
  short_description: "Verify Expo changes from a ready, known state"
  default_prompt: "Use $expo-dev-loop to prepare a ready Expo runtime and verify this change on one target."
