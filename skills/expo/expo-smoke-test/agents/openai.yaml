interface:
  display_name: "Expo Smoke Test"
  short_description: "Verify both platforms from explicit runtime state"
  default_prompt: "Use $expo-smoke-test to prepare explicit runtime state and verify this Expo change plus the core loop on iOS and Android."
