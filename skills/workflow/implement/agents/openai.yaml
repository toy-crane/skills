interface:
  display_name: "Implement"
  short_description: "Implement with per-outcome reconciliation and review"
  default_prompt: "Use $implement to implement this spec folder one outcome at a time, reload current repository evidence and reconcile unfinished tasks after each outcome, then complete verification, automated code review, and a runnable handoff when available."
