interface:
  display_name: "Build Prototype"
  short_description: "Align on UI by building every screen"
  default_prompt: "Use $build-prototype to build every screen of this feature as one dummy-data HTML file I react to: propose the screen list as a draft, build it in our own design system (or minimally if we have none) with realistic data and edge states, and walk me through it screen by screen. Close by saving the approved surface beside the spec."
