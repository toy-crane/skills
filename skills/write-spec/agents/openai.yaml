interface:
  display_name: "Write Spec"
  short_description: "Interview toward shared understanding"
  default_prompt: "Use $write-spec to interview me one decision per turn through drafts I correct; for material UI changes, inspect and verify the current surface, then batch unresolved experiential decisions into one review; maintain the glossary and decision records, and close with a spec the next session can implement from."
