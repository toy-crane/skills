interface:
  display_name: "Prototype"
  short_description: "Make one question directly judgeable"
  default_prompt: "Use $prototype when a product question must be seen or tried rather than argued: compare variants, treat my reaction as the answer, and fold the decision into the issue artifact."
