interface:
  display_name: "Prototype"
  short_description: "Align on UI by building every screen"
  default_prompt: "Use $prototype to build every screen of this feature as one dummy-data HTML file I react to: propose the screen list as a draft, walk me through a wireframe skeleton pass, then swap in the real design tokens and fill with realistic data and edge states. Close by saving the approved surface beside the spec."
